Replace this folder's favicon with a real one before launch.

Recommended: export a simplified version of the wildflower line-mark (see
components/marketing/Logo.tsx) as favicon.ico (place at project root: app/favicon.ico)
plus a 512x512 PNG for social/app icons.
